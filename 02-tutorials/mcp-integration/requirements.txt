# Pinned to versions verified to import together on Python 3.11+.
anthropic[vertex]==0.109.2
google-cloud-aiplatform[agent_engines]==1.158.0
google-adk==2.2.0
mcp==1.28.0
python-dotenv==1.2.2
ipykernel==7.3.0

# Note: google-adk 2.2.0 constrains opentelemetry-* to <=1.41.1, while
# google-cloud-aiplatform[agent_engines] may resolve a newer opentelemetry.
# The mismatch is harmless for the imports this tutorial uses; pip will print
# a resolver warning. Pin opentelemetry-api/opentelemetry-sdk to 1.41.1 if you
# want a clean resolve.
